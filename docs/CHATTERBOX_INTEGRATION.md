# Интеграция Chatterbox TTS в VideoLingo

## 📋 Описание

Chatterbox TTS — это open-source синтезатор речи (TTS) от Resemble AI с лицензией MIT, поддерживающий 23 языка и zero-shot voice cloning. Это первый production-grade open source TTS, превосходящий закрытые системы наподобие ElevenLabs.

## ✨ Преимущества

- 🌍 **23 языка**: Поддержка множества языков из коробки
- 🆓 **Open-source**: Полностью бесплатный, без API лимитов
- 🔒 **Локальное выполнение**: Полный контроль и приватность
- 🎭 **Voice Cloning**: Zero-shot клонирование голоса без обучения
- 🎚️ **Контроль экспрессии**: Уникальная настройка эмоциональности
- 🚀 **Production-grade**: Качество на уровне коммерческих решений

## 📦 Установка

### 1. Установка через pip (рекомендуется)

```bash
pip install chatterbox-tts soundfile
```

### 2. Установка из исходников

```bash
git clone https://github.com/resemble-ai/chatterbox.git
cd chatterbox
pip install -e .
```

## ⚙️ Настройка

### 1. Активация в config.yaml

Откройте `config.yaml` и измените `tts_method`:

```yaml
## ======================== Dubbing Settings ======================== ##
tts_method: 'chatterbox_tts'  # ← Измените на chatterbox_tts
```

### 2. Настройка параметров

```yaml
chatterbox_tts:
  # Режим клонирования голоса:
  # 1 = Без клонирования (быстро)
  # 2 = Один референс для всего видео (средне)
  # 3 = Отдельный референс для каждого сегмента (медленно, лучшее качество)
  voice_clone_mode: 1

  # Эмоциональность (0.0-1.0, рекомендуется 0.5)
  exaggeration: 0.5

  # Влияние референсного аудио для клонирования (0.3-0.5 оптимально)
  cfg_weight: 0.4

  # Устройство: 'cuda' для GPU, 'cpu' для CPU
  device: 'cuda'
```

## 🎯 Режимы работы

### Режим 1: Базовый TTS (без клонирования)

**Когда использовать:**
- Для быстрого тестирования
- Когда не нужен оригинальный голос
- Для максимальной скорости

**Особенности:**
- Использует встроенные голоса Chatterbox
- Самый быстрый режим
- Нет требований к референсному аудио

**Конфигурация:**
```yaml
chatterbox_tts:
  voice_clone_mode: 1
```

---

### Режим 2: Клонирование с единым референсом

**Когда использовать:**
- Нужен постоянный голос на всё видео
- Баланс качества и скорости
- Одинаковый спикер на протяжении видео

**Особенности:**
- Использует `output/audio/refers/1.wav`
- Референс создаётся автоматически из первого сегмента
- Постоянный голос на всё видео

**Конфигурация:**
```yaml
chatterbox_tts:
  voice_clone_mode: 2
  cfg_weight: 0.4  # Влияние референса
```

**Ручная настройка референса:**
```bash
# Поместите свой референсный аудиофайл в:
output/audio/refers/1.wav
```

---

### Режим 3: Клонирование для каждого сегмента

**Когда использовать:**
- Нужна максимальная точность клонирования
- Разные интонации в разных частях видео
- Лучшее качество

**Особенности:**
- Использует `output/audio/refers/{N}.wav` для каждого сегмента
- Референсы создаются автоматически из каждого сегмента
- Адаптируется к оригинальной интонации
- Самое медленное выполнение

**Конфигурация:**
```yaml
chatterbox_tts:
  voice_clone_mode: 3
  cfg_weight: 0.4
  exaggeration: 0.5
```

## 🌍 Поддерживаемые языки (23)

| Код языка | Название | Поддержка |
|-----------|----------|-----------|
| `en` | English | ✅ |
| `es` | Español | ✅ |
| `fr` | Français | ✅ |
| `de` | Deutsch | ✅ |
| `it` | Italiano | ✅ |
| `pt` | Português | ✅ |
| `pl` | Polski | ✅ |
| `tr` | Türkçe | ✅ |
| `ru` | Русский | ✅ |
| `nl` | Nederlands | ✅ |
| `cs` | Čeština | ✅ |
| `ar` | العربية | ✅ |
| `zh-cn` | 简体中文 | ✅ |
| `hu` | Magyar | ✅ |
| `ko` | 한국어 | ✅ |
| `ja` | 日本語 | ✅ |
| `hi` | हिन्दी | ✅ |
| `th` | ไทย | ✅ |
| `vi` | Tiếng Việt | ✅ |
| `id` | Bahasa Indonesia | ✅ |
| `he` | עברית | ✅ |
| `uk` | Українська | ✅ |
| `el` | Ελληνικά | ✅ |

## 🔧 Параметры тонкой настройки

### Exaggeration (Эмоциональность)

Контролирует эмоциональность и выразительность речи.

```yaml
exaggeration: 0.5  # Рекомендуемое значение
```

**Значения:**
- `0.0` - Монотонная, нейтральная речь
- `0.3` - Слегка выразительная
- `0.5` - **Сбалансированная (рекомендуется)**
- `0.7` - Выразительная
- `1.0` - Максимально эмоциональная

**Когда изменять:**
- Увеличить для драматического контента
- Уменьшить для технической документации

---

### CFG Weight (Влияние референса)

Контролирует, насколько сильно влияет референсное аудио на генерацию (только для режимов 2 и 3).

```yaml
cfg_weight: 0.4  # Рекомендуемое значение
```

**Значения:**
- `0.0` - Референс игнорируется (как режим 1)
- `0.3` - Слабое влияние референса
- `0.4` - **Сбалансированное (рекомендуется)**
- `0.5` - Сильное влияние референса
- `1.0` - Максимальное копирование референса

**Когда изменять:**
- Увеличить для более точного клонирования
- Уменьшить если клонирование слишком артефактное

## 💻 Системные требования

### Минимальные требования

- **CPU**: Любой современный процессор (для CPU режима)
- **RAM**: 4GB+ системной памяти
- **Место на диске**: 5GB (для моделей)
- **ОС**: Linux, Windows, macOS

### Рекомендуемые требования

- **GPU**: NVIDIA GPU с 4GB+ VRAM
- **CUDA**: 11.8+ или 12.x
- **RAM**: 8GB+ системной памяти
- **Место на диске**: 10GB

### Производительность

| Режим | GPU (RTX 3060) | CPU (i5) | Качество |
|-------|----------------|----------|----------|
| Режим 1 | ~2 сек/сегмент | ~10 сек/сегмент | Хорошее |
| Режим 2 | ~3 сек/сегмент | ~15 сек/сегмент | Отличное |
| Режим 3 | ~4 сек/сегмент | ~20 сек/сегмент | Превосходное |

## 🧪 Тестирование

### Быстрый тест

```bash
python test_chatterbox.py
```

Этот скрипт:
1. Проверит установку Chatterbox
2. Протестирует генерацию на разных языках
3. Проверит voice cloning (если есть референс)
4. Создаст тестовые аудио в `output/test_chatterbox/`

### Ручной тест

```python
from core.tts_backend.chatterbox_tts import chatterbox_tts

# Простая генерация
chatterbox_tts(
    text="Hello, this is a test!",
    save_path="test.wav",
    language_id="en",
    device="cuda"
)

# С клонированием голоса
chatterbox_tts(
    text="Hello, this is a voice clone test!",
    save_path="test_clone.wav",
    language_id="en",
    audio_prompt="output/audio/refers/1.wav",
    exaggeration=0.5,
    cfg_weight=0.4,
    device="cuda"
)
```

## 🐛 Решение проблем

### Проблема: "CUDA not available"

**Симптом:**
```
[yellow]CUDA not available, falling back to CPU[/yellow]
```

**Решение:**
1. Проверьте установку CUDA: `nvidia-smi`
2. Переустановите PyTorch с CUDA:
   ```bash
   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
   ```
3. Или используйте CPU режим:
   ```yaml
   device: 'cpu'
   ```

---

### Проблема: "Chatterbox TTS is not installed"

**Симптом:**
```
ImportError: Chatterbox TTS is not installed
```

**Решение:**
```bash
pip install chatterbox-tts soundfile
```

---

### Проблема: "Reference audio not found"

**Симптом:**
```
[yellow]Reference audio not found at output/audio/refers/1.wav, extracting...[/yellow]
```

**Решение:**
1. Сначала запустите видеообработку для извлечения референса
2. Или вручную поместите референсный WAV-файл в `output/audio/refers/1.wav`
3. Или используйте режим 1 (без клонирования)

---

### Проблема: Низкое качество голоса

**Решение:**
1. Увеличьте `cfg_weight` до 0.5
2. Используйте режим 3 для лучшего клонирования
3. Проверьте качество референсного аудио
4. Настройте `exaggeration` под ваш контент

---

### Проблема: Слишком медленная генерация

**Решение:**
1. Используйте GPU вместо CPU
2. Переключитесь на режим 1 (без клонирования)
3. Уменьшите разрешение видео
4. Используйте меньше сегментов

## 📊 Сравнение с другими TTS

| Характеристика | Chatterbox | Azure TTS | OpenAI TTS | GPT-SoVITS |
|----------------|------------|-----------|------------|------------|
| Стоимость | Бесплатно | Платно | Платно | Бесплатно |
| Языки | 23 | 100+ | 50+ | 2 (zh, en) |
| Voice Cloning | ✅ Zero-shot | ❌ Нет | ❌ Нет | ✅ Требует обучения |
| Локальное выполнение | ✅ Да | ❌ Только API | ❌ Только API | ✅ Да |
| Качество | Отличное | Отличное | Отличное | Отличное |
| Скорость (GPU) | Быстро | Очень быстро | Очень быстро | Средне |
| Контроль экспрессии | ✅ Да | Ограниченный | Ограниченный | ✅ Да |
| Требования GPU | 4GB+ | Нет | Нет | 8GB+ |

## 🎓 Лучшие практики

### 1. Выбор режима

- **Режим 1**: Для быстрых тестов и предпросмотров
- **Режим 2**: Для финального видео с постоянным голосом
- **Режим 3**: Для профессионального дубляжа с высокой точностью

### 2. Настройка параметров

```yaml
# Для технического контента (документация, обучение)
exaggeration: 0.3
cfg_weight: 0.4

# Для развлекательного контента (видеоблоги, стримы)
exaggeration: 0.6
cfg_weight: 0.4

# Для драматического контента (фильмы, аудиокниги)
exaggeration: 0.7
cfg_weight: 0.5
```

### 3. Оптимизация референсного аудио

Для лучшего качества клонирования:
- Используйте чистое аудио без фонового шума
- Референс должен быть 3-10 секунд
- Избегайте музыки и эффектов в референсе
- Используйте моно аудио 16kHz+

### 4. Производительность

```yaml
# Максимальная скорость (для тестов)
voice_clone_mode: 1
device: 'cuda'

# Баланс качества и скорости (рекомендуется)
voice_clone_mode: 2
device: 'cuda'
exaggeration: 0.5
cfg_weight: 0.4

# Максимальное качество (для финального рендера)
voice_clone_mode: 3
device: 'cuda'
exaggeration: 0.5
cfg_weight: 0.5
```

## 📚 Дополнительные ресурсы

- **GitHub Chatterbox**: https://github.com/resemble-ai/chatterbox
- **VideoLingo Docs**: /docs/
- **Тестовый скрипт**: `test_chatterbox.py`
- **Модуль интеграции**: `core/tts_backend/chatterbox_tts.py`

## 🤝 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи в консоли
2. Запустите `python test_chatterbox.py`
3. Создайте issue на GitHub VideoLingo
4. Проверьте документацию Chatterbox

---

**Автор интеграции**: VideoLingo Team
**Версия**: 1.0.0
**Дата**: 2025-12-14
